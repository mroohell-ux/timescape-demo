<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Base: brighter sky gradient -->
    <item>
        <shape android:shape="rectangle">
            <gradient
                android:type="linear"
                android:angle="315"
                android:startColor="#E0ECFF"
                android:endColor="#BEDCFF"/>  <!-- airy blue with more color -->
        </shape>
    </item>

    <!-- Warm sun glow (top-right) -->
    <item android:gravity="top|end">
        <shape android:shape="rectangle">
            <size android:width="900dp" android:height="900dp"/>
            <gradient
                android:type="radial"
                android:centerX="78%"
                android:centerY="10%"
                android:gradientRadius="520dp"
                android:startColor="#80FFD89A"
                android:endColor="#00FFD89A"/>
        </shape>
    </item>

    <!-- Fresh mint bloom (bottom-left) -->
    <item android:gravity="bottom|start">
        <shape android:shape="rectangle">
            <size android:width="900dp" android:height="900dp"/>
            <gradient
                android:type="radial"
                android:centerX="16%"
                android:centerY="86%"
                android:gradientRadius="560dp"
                android:startColor="#66A8FFE0"
                android:endColor="#00A8FFE0"/>
        </shape>
    </item>

</layer-list>
