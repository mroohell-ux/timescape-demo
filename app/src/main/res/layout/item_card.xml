<?xml version="1.0" encoding="utf-8"?>
<com.example.timescapedemo.AspectRatioCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/card"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:clipToOutline="true"
    app:cardUseCompatPadding="false"
    app:cardCornerRadius="20dp"
    app:cardElevation="12dp"
    app:cardBackgroundColor="@android:color/transparent"
    app:strokeColor="#33FFFFFF"
    app:strokeWidth="1dp">

    <!-- ConstraintLayout so bg layer doesn't affect measured height -->
    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <!-- Liquid glass overlay driven by Kyant's shader -->
        <com.example.timescapedemo.liquid.LiquidGlassLayout
            android:id="@+id/liquidGlass"
            android:layout_width="0dp"
            android:layout_height="0dp"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent" />

        <!-- Content drives height; LayoutManager caps to 2/3 screen -->
        <com.example.timescapedemo.MaxHeightLinearLayout
            android:id="@+id/card_content"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="16dp"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent">

            <TextView
                android:id="@+id/time"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="12h ago"
                android:textColor="#88555555"
                android:textSize="12sp"/>

            <!-- Title stays one line and autoshrinks so numbers never clip -->
            <TextView
                android:id="@+id/title"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="6dp"
                android:text="Contact"
                android:textStyle="bold"
                android:ellipsize="end"
                android:maxLines="1"
                app:autoSizeTextType="uniform"
                app:autoSizeMinTextSize="18sp"
                app:autoSizeMaxTextSize="27sp"
                app:autoSizeStepGranularity="1sp"/>

            <TextView
                android:id="@+id/snippet"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="6dp"
                android:text="Snippet"
                android:textSize="14sp"
                android:ellipsize="end"/>
        </com.example.timescapedemo.MaxHeightLinearLayout>
    </androidx.constraintlayout.widget.ConstraintLayout>
</com.example.timescapedemo.AspectRatioCardView>
